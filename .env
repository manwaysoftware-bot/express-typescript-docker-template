environment=dev
#is uset into docker author name is used for container.is set author name in side dockerfile using docker compose
developerName=ragul-dev
# Maimum project or microservive name this use give servie name in docker always used image name and container name
serviceName= centerl
#version before deploy this verion number change is used docker image label and tag 
version=3.1.0
#internally runned port in our application is used as also external ip address approach from docker
port=1000
